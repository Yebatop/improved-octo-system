/**
 * Read-only mixin of the event timers, listed in skirmish-evtimers.mixins.json: server block updates
 * ({@code ClientLevel.setServerVerifiedBlockState}). Nothing is sent, cancelled or changed.
 */
package dev.skirmish.module.evtimers.mixin;
