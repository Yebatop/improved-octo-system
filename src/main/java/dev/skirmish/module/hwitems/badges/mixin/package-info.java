/**
 * Render-only mixin of the Item Badges module, listed in skirmish-hwitems.mixins.json: marks when vanilla draws the
 * hotbar ({@code Gui.renderItemHotbar}) so hotbar badges can be switched off separately. The badges themselves are
 * drawn through Fabric's DrawItemStackOverlayCallback.
 */
package dev.skirmish.module.hwitems.badges.mixin;
