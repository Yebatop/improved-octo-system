/**
 * Read-only mixins of the item timers, listed in skirmish-hwtimers.mixins.json: title/subtitle/action-bar text
 * ({@code Gui}), server block updates ({@code ClientLevel.setServerVerifiedBlockState}) and explosions
 * ({@code ClientPacketListener.handleExplosion}). Nothing is sent, cancelled or changed.
 */
package dev.skirmish.module.hwtimers.mixin;
