/**
 * Render-only mixin of the TNT timer, listed in skirmish-tnttimer.mixins.json: two TAIL hooks on
 * {@code TntRenderer} (render-state extraction and submit). Nothing is sent or changed.
 */
package dev.skirmish.module.tnttimer.mixin;
