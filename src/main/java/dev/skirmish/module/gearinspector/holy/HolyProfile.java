package dev.skirmish.module.gearinspector.holy;

/** Setting «Профиль HolyWorld» of GearInspector. */
public enum HolyProfile {
    /** HolyWorld item knowledge is used while connected to HolyWorld ({@code HolyWorld.isConnected()}). */
    AUTO,
    /** Never: the panel shows only what vanilla components say. */
    OFF
}
