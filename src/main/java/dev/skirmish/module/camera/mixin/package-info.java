/**
 * Render-only mixin of the Low fire module, listed in skirmish-camera.mixins.json: position and alpha of the
 * first-person fire overlay (ScreenEffectRenderer.renderFire). Camera comfort itself needs no mixin: it writes
 * vanilla options.
 */
package dev.skirmish.module.camera.mixin;
