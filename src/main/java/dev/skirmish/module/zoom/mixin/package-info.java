/**
 * Mixins of the Zoom module, listed in skirmish-zoom.mixins.json: the world FOV (GameRenderer.getFov), the camera
 * turn speed and the mouse wheel while zooming (MouseHandler), and hiding the hands (ItemInHandRenderer). No option
 * is written, no key is pressed and nothing is sent.
 */
package dev.skirmish.module.zoom.mixin;
